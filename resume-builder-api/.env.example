# Core
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Database
DATABASE_URL=postgresql://postgres:postgres@postgresql://postgres.zjxeyicrmbnxmxbqzhvi:postgres@aws-1-ap-south-1.pooler.supabase.com:6543/postgres/resume_builder?schema=public
PRISMA_CONNECTION_LIMIT=10
PRISMA_POOL_TIMEOUT_SECONDS=30
PRISMA_CONNECT_TIMEOUT_SECONDS=10

# Auth
JWT_SECRET=change_me_super_secret
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_REFRESH_EXPIRES_IN=30d
OTP_SESSION_EXPIRES_IN=30m
SMS_PROVIDER=dev
OTP_SEND_LIMIT_PER_PHONE_PER_HOUR=5
OTP_SEND_LIMIT_PER_IP_PER_HOUR=20
OTP_VERIFY_LIMIT_PER_PHONE_PER_HOUR=20
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
TWILIO_HTTP_TIMEOUT_MS=10000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback
GOOGLE_OAUTH_SUCCESS_REDIRECT=http://localhost:3000/dashboard
DRIVE_SESSION_TTL_MS=1800000
DRIVE_OAUTH_STATE_TTL_MS=600000

# ─── Social Login (Google/LinkedIn/Yahoo) ─────────────────────────────────
# Google Login (separate from Google Drive OAuth above)
GOOGLE_LOGIN_CLIENT_ID=
GOOGLE_LOGIN_CLIENT_SECRET=
GOOGLE_LOGIN_REDIRECT_URI=http://localhost:3001/auth/social/google/callback

# LinkedIn Login
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=http://localhost:3001/auth/social/linkedin/callback

# Yahoo Login
YAHOO_CLIENT_ID=
YAHOO_CLIENT_SECRET=
YAHOO_REDIRECT_URI=http://localhost:3001/auth/social/yahoo/callback

# GitHub Login
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:3001/auth/social/github/callback

# Social Login Success Redirect
SOCIAL_LOGIN_SUCCESS_URL=http://localhost:3000/auth/callback
REDIS_URL=https://YOUR-UPSTASH-URL.upstash.io
REDIS_TOKEN=YOUR_UPSTASH_TOKEN
TOKEN_ENC_KEY=00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff
# ─── Email / SMTP (required for OTP delivery) ─────────────────────────────
# Configure ANY SMTP provider below. OTP emails can be delivered to
# any valid email address (Gmail, Outlook, Yahoo, corporate, etc.)
# as long as the sender SMTP account is properly configured.
#
# ── Gmail SMTP ──
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=you@gmail.com
# SMTP_PASS=abcd-efgh-ijkl-mnop        (use App Password, not account password)
# SMTP_FROM=ATS Resume Builder <you@gmail.com>
# Note: Enable 2FA then create App Password at https://myaccount.google.com/apppasswords
#
# ── Outlook / Microsoft 365 SMTP ──
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=you@outlook.com
# SMTP_PASS=your-password
# SMTP_FROM=ATS Resume Builder <you@outlook.com>
#
# ── Zoho SMTP ──
# SMTP_HOST=smtp.zoho.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=you@zoho.com
# SMTP_PASS=your-password
# SMTP_FROM=ATS Resume Builder <you@zoho.com>
#
# ── Amazon SES SMTP ──
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-user
# SMTP_PASS=your-ses-smtp-password
# SMTP_FROM=ATS Resume Builder <verified@yourdomain.com>
#
# ── SendGrid SMTP ──
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=SG.your-sendgrid-api-key
# SMTP_FROM=ATS Resume Builder <you@yourdomain.com>
#
# ── Custom / Corporate SMTP ──
# SMTP_HOST=mail.yourcompany.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@yourcompany.com
# SMTP_PASS=your-password
# SMTP_FROM=ATS Resume Builder <noreply@yourcompany.com>
#
# Active configuration (replace with your real values):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=ATS Resume Builder <your-email@gmail.com>
SMTP_SECURE=false
# SMTP_CONNECTION_TIMEOUT_MS=10000
# SMTP_GREETING_TIMEOUT_MS=10000

SMS_GATEWAY_URL=http://localhost:7071
GSM_MODEM_ENABLED=false
GSM_MODEM_COMMAND=

# AI service (legacy — used by existing parse-jd, critique, skill-gap endpoints)
AI_SERVICE_URL=http://localhost:7001

# AI Provider for AI-powered critique (default: groq)
# Options: groq, xai
AI_PROVIDER=groq
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
# XAI_API_KEY=
# XAI_MODEL=grok-3-mini-fast
AI_TIMEOUT_MS=30000
AI_FREE_MAX_REQUESTS_PER_DAY=10

# Stripe
STRIPE_SECRET_KEY=sk_test_change_me
STRIPE_WEBHOOK_SECRET=whsec_change_me
STRIPE_PRICE_STUDENT=price_student_id
STRIPE_PRICE_PRO=price_pro_id
STRIPE_SUCCESS_URL=http://localhost:3000/dashboard
STRIPE_CANCEL_URL=http://localhost:3000/dashboard

# Admin settings access
ADMIN_EMAILS=novaai0401@gmail.com
ADMIN_USER_IDS=
ADMIN_MOBILES=+919307009427

# Resume creation rate-limit feature rollout
RESUME_CREATION_RATE_LIMIT_DEFAULT=false
FORCE_DISABLE_RATE_LIMIT=true

# PDF export — Chrome/Chromium path for Puppeteer (auto-detected if not set)
# CHROME_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
