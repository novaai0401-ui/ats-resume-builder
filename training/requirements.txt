# Pinned to versions that work on Google Colab free tier (Python 3.10+,
# CUDA 12.1 T4) and on CPU for local smoke tests. Heaviest dep is
# transformers; everything else is small.

torch>=2.2,<2.6
transformers>=4.40,<4.50
datasets>=2.18,<3.0
accelerate>=0.30,<1.2
seqeval>=1.2.2,<2.0
numpy>=1.24,<2.2
pydantic>=2.5,<3.0
# Optional: only needed for evaluation upload to the API.
requests>=2.31,<3.0
